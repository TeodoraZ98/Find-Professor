import { makeStyles } from '@material-ui/styles';
const classStyles = makeStyles({
  divKartica: {
    display: 'flex',
    margin: 5,
    flex: 1,
  },

  divVreme: {
    display: 'flex',
    justifyContent: 'end',
  },
});
export default classStyles;
